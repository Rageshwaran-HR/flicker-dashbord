# Firestore seeding script env
# Copy to .env.seed (or set vars in your terminal) before running npm run seed:firestore

# Your Firebase project id
FIREBASE_PROJECT_ID="fliker-5c335"

# Option A (recommended): path to service account json
# FIREBASE_SERVICE_ACCOUNT_PATH="./service-account.json"

# Option B: full JSON in one line (escape newlines if needed)
# FIREBASE_SERVICE_ACCOUNT_JSON='{ "type": "service_account", ... }'

# Optional: custom seed file path
# SEED_DATA_PATH="./data/firestore.sample.json"
